# 碳中和数字碳资产管理系统 - 后续开发规划

## 🎯 项目当前状态
- **完成度**: 75%
- **核心业务**: 已完整实现
- **已实现**: 7个微服务、10张数据库表、50+接口
- **待完善**: 文档管理、交易撮合、系统管理、安全认证

---

## 🚀 第一阶段：核心功能完善（1-2周，优先级P0）

### 1.1 开发工作台文档管理 ⭐⭐⭐⭐⭐
**目标**: 实现项目文档的完整生命周期管理

**后端开发** (service-development):
```sql
-- 新增表: project_document
CREATE TABLE project_document (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  project_id BIGINT NOT NULL COMMENT '项目ID',
  doc_name VARCHAR(200) NOT NULL COMMENT '文档名称',
  doc_type VARCHAR(50) COMMENT '文档类型(设计文档/监测报告/核查报告)',
  file_url VARCHAR(500) COMMENT '文件URL',
  file_size BIGINT COMMENT '文件大小(bytes)',
  version VARCHAR(20) DEFAULT 'v1.0' COMMENT '版本号',
  upload_user_id BIGINT COMMENT '上传人',
  status VARCHAR(20) DEFAULT 'DRAFT' COMMENT '状态(DRAFT/REVIEWING/APPROVED)',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  update_time DATETIME
);
```

**需实现接口** (8个):
- `POST /api/development/document/upload` - 上传文档
- `GET /api/development/document/list` - 文档列表（分页）
- `GET /api/development/document/{id}` - 文档详情
- `GET /api/development/document/download/{id}` - 下载文档
- `PUT /api/development/document/{id}` - 更新文档信息
- `DELETE /api/development/document/{id}` - 删除文档
- `POST /api/development/document/{id}/version` - 创建新版本
- `GET /api/development/document/project/{projectId}` - 按项目查询

**前端开发**:
- 完善 `workbench.vue` 的上传组件（已有el-upload框架）
- 实现文件列表展示、下载、删除功能
- 增加版本管理界面

**预计工作量**: 3天

---

### 1.2 交易撮合引擎优化 ⭐⭐⭐⭐⭐
**目标**: 实现自动匹配买卖单，提升交易效率

**需修复的TODO**:
1. `TradeOrderController.java:49` - 添加资产校验
2. `QuoteOrderServiceImpl.java:120` - 实现交易流程触发

**功能实现**:
```java
// 1. 资产校验服务（新增Feign Client）
@FeignClient("service-assets")
public interface AssetFeignClient {
    @GetMapping("/api/assets/validate")
    Result<Boolean> validateAsset(@RequestParam Long userId, 
                                   @RequestParam String assetType,
                                   @RequestParam BigDecimal quantity);
}

// 2. 自动撮合服务
public class TradeMatchService {
    // 价格优先、时间优先原则
    public void matchOrders() {
        // 查询所有OPEN状态的买单和卖单
        // 按价格排序，买单从高到低，卖单从低到高
        // 匹配逻辑：买单价格 >= 卖单价格
        // 成交后更新订单状态、账户余额、资产持仓
    }
}

// 3. 定时任务
@Scheduled(fixedRate = 5000) // 每5秒执行一次
public void autoMatch() {
    tradeMatchService.matchOrders();
}
```

**数据库变更**:
```sql
-- 新增字段到 trade_order 表
ALTER TABLE trade_order 
ADD COLUMN matched_order_id BIGINT COMMENT '匹配的订单ID',
ADD COLUMN match_time DATETIME COMMENT '成交时间',
ADD COLUMN final_price DECIMAL(20,2) COMMENT '最终成交价';
```

**预计工作量**: 4天

---

### 1.3 用户登录状态管理 ⭐⭐⭐⭐
**目标**: 解决前端多处硬编码userId=1的问题

**需修复的TODO**:
- `quota.vue:101` - 从登录信息中获取userId
- `credit.vue:128` - 从登录信息中获取userId
- `company.vue:132` - 从登录信息获取userId
- `company.vue:160` - 从登录信息获取userId

**实现方案**:
```javascript
// 1. 创建 store/user.js
import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({
    userId: null,
    username: '',
    token: '',
    roles: []
  }),
  actions: {
    setUserInfo(userInfo) {
      this.userId = userInfo.id
      this.username = userInfo.username
      this.token = userInfo.token
    },
    logout() {
      this.userId = null
      this.token = ''
      localStorage.removeItem('token')
    }
  },
  getters: {
    isLoggedIn: (state) => !!state.token
  }
})

// 2. 在各个页面中使用
import { useUserStore } from '@/store/user'
const userStore = useUserStore()
const userId = userStore.userId
```

**预计工作量**: 1天

---

## 🎨 第二阶段：系统管理模块（2-3周，优先级P1）

### 2.1 角色权限管理（RBAC）⭐⭐⭐⭐

**数据库设计**:
```sql
-- 角色表
CREATE TABLE sys_role (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  role_name VARCHAR(50) NOT NULL COMMENT '角色名称',
  role_code VARCHAR(50) NOT NULL COMMENT '角色编码',
  description VARCHAR(200) COMMENT '角色描述',
  status TINYINT DEFAULT 1 COMMENT '状态:0-禁用 1-启用',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 菜单权限表
CREATE TABLE sys_menu (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  parent_id BIGINT DEFAULT 0 COMMENT '父菜单ID',
  menu_name VARCHAR(50) NOT NULL COMMENT '菜单名称',
  menu_path VARCHAR(200) COMMENT '路由路径',
  component VARCHAR(200) COMMENT '组件路径',
  permission VARCHAR(100) COMMENT '权限标识',
  menu_type CHAR(1) COMMENT '菜单类型:M-目录 C-菜单 F-按钮',
  icon VARCHAR(100) COMMENT '图标',
  order_num INT DEFAULT 0 COMMENT '排序',
  status TINYINT DEFAULT 1 COMMENT '状态',
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

-- 角色菜单关联表
CREATE TABLE sys_role_menu (
  role_id BIGINT NOT NULL,
  menu_id BIGINT NOT NULL,
  PRIMARY KEY (role_id, menu_id)
);

-- 用户角色关联表
CREATE TABLE sys_user_role (
  user_id BIGINT NOT NULL,
  role_id BIGINT NOT NULL,
  PRIMARY KEY (user_id, role_id)
);
```

**功能接口**:
- 角色管理：增删改查、分配权限
- 菜单管理：树形结构、动态路由
- 用户角色：批量分配、权限校验

**预计工作量**: 5天

---

### 2.2 字典配置管理 ⭐⭐⭐

**数据库设计**:
```sql
CREATE TABLE sys_dict_type (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  dict_name VARCHAR(100) NOT NULL COMMENT '字典名称',
  dict_type VARCHAR(100) NOT NULL COMMENT '字典类型',
  status TINYINT DEFAULT 1,
  remark VARCHAR(500),
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE sys_dict_data (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  dict_type VARCHAR(100) NOT NULL COMMENT '字典类型',
  dict_label VARCHAR(100) NOT NULL COMMENT '字典标签',
  dict_value VARCHAR(100) NOT NULL COMMENT '字典键值',
  dict_sort INT DEFAULT 0 COMMENT '排序',
  status TINYINT DEFAULT 1,
  create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

**应用场景**:
- 资产类型（QUOTA/CREDIT）
- 订单状态（OPEN/CLOSED/CANCELLED）
- 认证状态（未认证/审核中/已认证/已驳回）
- 项目阶段（立项/实施/验收）

**预计工作量**: 3天

---

### 2.3 审批流程引擎 ⭐⭐⭐⭐

**技术选型**: Flowable（轻量级工作流引擎）

**集成步骤**:
```xml
<!-- pom.xml -->
<dependency>
    <groupId>org.flowable</groupId>
    <artifactId>flowable-spring-boot-starter</artifactId>
    <version>6.8.0</version>
</dependency>
```

**审批场景**:
1. 企业认证审批（已有表，需接入流程）
2. 项目立项审批
3. 文档审核流程
4. 大额交易审批

**流程定义示例**:
```xml
<!-- company_approval.bpmn20.xml -->
<process id="companyApproval" name="企业认证审批">
  <startEvent id="start"/>
  <userTask id="reviewTask" name="审核" assignee="${reviewer}"/>
  <exclusiveGateway id="decision"/>
  <userTask id="secondReview" name="二次审核"/>
  <endEvent id="approved" name="通过"/>
  <endEvent id="rejected" name="驳回"/>
</process>
```

**预计工作量**: 7天

---

## 🔐 第三阶段：安全性增强（1-2周，优先级P1）

### 3.1 JWT统一认证 ⭐⭐⭐⭐⭐

**当前问题**: 
- Token存储在Redis，但未标准化
- 缺少Token刷新机制
- 无统一的权限拦截

**解决方案**:
```java
// 1. JWT工具类
public class JwtUtils {
    public String generateToken(Long userId, String username, List<String> roles) {
        return Jwts.builder()
            .setSubject(username)
            .claim("userId", userId)
            .claim("roles", roles)
            .setExpiration(new Date(System.currentTimeMillis() + 7200000)) // 2小时
            .signWith(SignatureAlgorithm.HS512, SECRET_KEY)
            .compact();
    }
    
    public Claims parseToken(String token) {
        return Jwts.parser()
            .setSigningKey(SECRET_KEY)
            .parseClaimsJws(token)
            .getBody();
    }
}

// 2. 认证拦截器
@Component
public class JwtAuthenticationFilter extends OncePerRequestFilter {
    @Override
    protected void doFilterInternal(HttpServletRequest request, 
                                   HttpServletResponse response,
                                   FilterChain filterChain) {
        String token = request.getHeader("Authorization");
        if (token != null && token.startsWith("Bearer ")) {
            String jwt = token.substring(7);
            Claims claims = jwtUtils.parseToken(jwt);
            // 设置Security上下文
        }
        filterChain.doFilter(request, response);
    }
}

// 3. 权限注解
@PreAuthorize("hasRole('ADMIN')")
@GetMapping("/admin/users")
public Result<?> listUsers() { ... }
```

**预计工作量**: 3天

---

### 3.2 接口权限控制 ⭐⭐⭐⭐

**实现方案**:
```java
// 1. 自定义注解
@Target(ElementType.METHOD)
@Retention(RetentionPolicy.RUNTIME)
public @interface RequirePermission {
    String value(); // 权限标识，如 "trade:order:create"
}

// 2. AOP切面
@Aspect
@Component
public class PermissionAspect {
    @Around("@annotation(requirePermission)")
    public Object checkPermission(ProceedingJoinPoint point, 
                                  RequirePermission requirePermission) {
        // 获取当前用户
        // 检查是否拥有对应权限
        // 无权限抛出异常
        return point.proceed();
    }
}

// 3. 使用示例
@RequirePermission("trade:order:create")
@PostMapping("/publish")
public Result<Boolean> publishOrder(@RequestBody TradeOrder order) {
    // ...
}
```

**预计工作量**: 2天

---

## 📊 第四阶段：运维监控（1-2周，优先级P2）

### 4.1 Spring Boot Admin 服务监控 ⭐⭐⭐

**功能**:
- 服务健康检查
- 内存/CPU使用情况
- HTTP请求追踪
- 日志查看

**集成步骤**:
```xml
<!-- 新建 carbon-admin 模块 -->
<dependency>
    <groupId>de.codecentric</groupId>
    <artifactId>spring-boot-admin-starter-server</artifactId>
</dependency>
```

**预计工作量**: 2天

---

### 4.2 ELK日志收集 ⭐⭐⭐

**架构**:
- Elasticsearch: 日志存储
- Logstash: 日志收集
- Kibana: 日志可视化

**日志格式**:
```json
{
  "timestamp": "2026-01-07T14:30:00",
  "service": "service-trade",
  "level": "INFO",
  "userId": 1,
  "action": "CREATE_ORDER",
  "message": "用户创建交易订单",
  "requestId": "req-123456",
  "duration": 125
}
```

**预计工作量**: 3天

---

### 4.3 性能监控 ⭐⭐⭐

**Prometheus + Grafana**:
- 接口响应时间
- 数据库连接池状态
- Redis命中率
- JVM内存使用

**预计工作量**: 3天

---

## 🐳 第五阶段：容器化部署（1周，优先级P2）

### 5.1 Docker化 ⭐⭐⭐

**Dockerfile示例**:
```dockerfile
FROM openjdk:21-jdk-slim
WORKDIR /app
COPY target/*.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**docker-compose.yml**:
```yaml
version: '3.8'
services:
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: 123456
  
  redis:
    image: redis:7.0
  
  nacos:
    image: nacos/nacos-server:v2.2.0
    
  service-provider:
    build: ./service-provider
    depends_on:
      - mysql
      - redis
      - nacos
```

**预计工作量**: 3天

---

## 📈 开发进度甘特图

| 阶段 | 任务 | 工作量 | 优先级 | 依赖 |
|------|------|--------|--------|------|
| **第一阶段** | 文档管理 | 3天 | P0 | 无 |
| | 交易撮合 | 4天 | P0 | 无 |
| | 登录状态 | 1天 | P0 | 无 |
| **第二阶段** | 角色权限 | 5天 | P1 | 登录状态 |
| | 字典管理 | 3天 | P1 | 无 |
| | 审批流程 | 7天 | P1 | 角色权限 |
| **第三阶段** | JWT认证 | 3天 | P1 | 角色权限 |
| | 接口权限 | 2天 | P1 | JWT认证 |
| **第四阶段** | 服务监控 | 2天 | P2 | 无 |
| | 日志收集 | 3天 | P2 | 无 |
| | 性能监控 | 3天 | P2 | 无 |
| **第五阶段** | Docker化 | 3天 | P2 | 无 |

**总工作量**: 约 6-8周

---

## 🎯 快速启动建议（本周目标）

建议按以下顺序开始：

### Week 1 (本周)
1. **Day 1-2**: 实现用户登录状态管理（修复所有TODO）
2. **Day 3-5**: 开发文档管理功能（后端+前端）

### Week 2
1. **Day 1-4**: 实现交易撮合引擎
2. **Day 5**: 测试与优化

### Week 3-4
1. 角色权限管理（5天）
2. 字典配置（3天）
3. JWT认证（3天）

---

## 📚 技术债务清单

1. **代码规范**: 
   - 统一异常处理机制（@ControllerAdvice）
   - 统一分页参数（PageRequest）
   - 统一日期格式（LocalDateTime）

2. **性能优化**:
   - 为高频查询表添加索引
   - Redis缓存热点数据
   - 前端路由懒加载

3. **测试覆盖**:
   - 单元测试（目前0%）
   - 集成测试
   - 压力测试

---

## ✅ 验收标准

### 第一阶段验收:
- ✅ 文档管理：能上传、下载、版本管理
- ✅ 交易撮合：自动匹配买卖单，正确成交
- ✅ 登录状态：所有页面从store获取userId

### 最终验收:
- ✅ 功能完成度达到 **95%**
- ✅ 代码测试覆盖率 > 60%
- ✅ 接口响应时间 < 500ms
- ✅ 系统可用性 > 99%
- ✅ Docker一键部署成功
